const { add } = require('./calculator');

test('add works', () => {
  expect(add(2, 3)).toBe(5);
});
const { add } = require('./calculator');

test('add works', () => {
  expect(add(2, 3)).toBe(5);
});
